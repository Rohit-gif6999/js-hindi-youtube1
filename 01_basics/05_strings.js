const name = "rohit"
const repo = 50
// console.log(name+repo+" value");    // old method



console.log(`Hello my name is ${name} and my repo count is ${repo}`);

const gameName = new String('rohithc')

console.log(gameName[0]);
console.log(gameName.__proto__);

log
